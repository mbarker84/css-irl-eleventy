{% extends "./layout.njk" %}

{% block content %}
  <div class="page-content page-content--page">
    <div class="container">
      <h1>{{ pageTitle }}</h1>
      {{ content | safe }}
    </div>
  </div>
{% endblock %}
